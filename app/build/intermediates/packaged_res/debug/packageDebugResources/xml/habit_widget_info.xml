<?xml version="1.0" encoding="utf-8"?>
<!--
    AppWidget metadata for the 2x3 Today widget.

    minWidth/minHeight match a 2-column × 3-row grid on a 4×4 launcher
    (~70dp per cell × 2 + padding for width, × 3 + padding for height).
    targetCellWidth/Height ask the launcher to allocate exactly 2×3 cells.
    resizeMode lets the user expand it horizontally or vertically; Glance
    re-renders to fill whatever real estate is given.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="170dp"
    android:minHeight="250dp"
    android:targetCellWidth="2"
    android:targetCellHeight="3"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:configure="dev.matejgroombridge.habittracker.widget.HabitWidgetConfigActivity"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/glance_default_loading_layout"
    android:description="@string/widget_description" />
